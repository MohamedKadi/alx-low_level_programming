#include <stdio.h>
/**
 * main - this program writs the alphabet in lowercase
 * Return: Always 0
 */
int main(void)
{
	char a;

	a = 'a';
	while
		(a <= 'a') {
			putchar(a);
			a++;
		}
	return (0);
}
