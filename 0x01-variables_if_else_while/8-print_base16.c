#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */
int main(void)
{
	char a;

	int b;

	a = 'a';
	while
		(b < 10) {
			putchar(b + '0');
			b++;
		}
	while
		(a < 'g') {
			putchar(a);
			a++;
		}
	putchar('\n');
	return (0);
}
